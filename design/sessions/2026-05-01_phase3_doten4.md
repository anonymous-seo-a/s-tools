# Phase 3 詳細設計 論点4: サイト全体監査レイヤー設計

日付: 2026-05-01
ステータス: 確定（Daiki 判断委任 → Claude が真=美フレームワーク + 警戒バイアスに基づき確定）
反映先: knowledge/05_rewrite_system_design.md（V-G章新設 / V章 26テーブル化 / V-A章 master_site_audit_score SQL）

---

## 0. 経緯

論点2 確定後、論点4 に進行（論点5 は最後）。
案E 旧軸4「構造的健全性」が削除され、別レイヤーに分離（IX 章既述）。
本論点でその分離先を確定。

---

## 1. 確定事項サマリ

| サブ論点 | 確定 |
|---|---|
| 4-1 責任境界 | 個別記事レベル vs サイト全体レベルの分離 |
| 4-2 γ 計算 | Google Knowledge Graph API + Jaccard 係数 (Adapter パターン) |
| 4-3 Topical 整合性 | config.js の SITE_TOPIC_ENTITIES 定数 (master_site_topic は却下) |
| 4-4 検出 3パターン | cannibalization / topical_drift / reputation_abuse_risk |
| 4-5 実装 | master_site_audit_score 新規テーブル + リライト履歴サブタブ |

全テーブル数: 25 → **26**

---

## 2. 4-1: 責任境界

```
個別記事レベル（リライト判定 / リライトキュー）:
  master_rewrite_target_score (4軸独立キュー)
  master_information_gain_score
  master_hcu_checklist
  master_rewrite_diff (差分単位)
  → 単一記事の品質改善

サイト全体レベル（リライト履歴 > サイト全体監査サブタブ）:
  master_article_similarity (α text / β query / γ entity)
  master_site_audit_score (新規、論点4 で確定)
  → 複数記事間の関係（カニバリ / トピック / Site Reputation Abuse）
```

---

## 3. 4-2: γ (entity_overlap) 計算ロジック

### 3案の評価

```
(A) Google Knowledge Graph API + Jaccard 係数  Claude 推奨
(B) LLM (Sonnet 4.6) でエンティティ抽出 + Jaccard
(C) embedding ベース (Voyage AI)
```

### Claude 推奨: (A) Knowledge Graph API

```
理由:
- Google 公式エンティティ定義との整合（YMYL 信頼性）
- entity_overlap = エンティティ集合の重なり、Jaccard が真=美の定義
- α text_similarity と機能分離（embedding は意味類似度、別概念）
- 無料枠 1000 query/day、月次バッチ + 主要記事のみで抑制
```

### 反証

```
反証: 「Google KG API は将来終了する可能性は？」
  → Adapter パターンで吸収（案D 3.H Archive Adapter と同パターン）。
    将来 KG API が終了したら別の Entity 抽出 API に差し替え可能。

反証: 「(C) embedding で十分では？」
  → α text_similarity と γ entity_overlap は別概念。
    α は文章類似（文体・語彙）、γ はエンティティ集合の重なり（意味）。
    両方持つことで「文章は似ているがトピックは違う」「文章は違うがトピックは同じ」
    の両方を検出可能。
```

実装位置: `shared/entity-extractor-adapter.js`（新規、Phase 4）

---

## 4. 4-3: Topical 整合性スコアの計算

### 主題定義の選択肢

```
(A) config.js 定数で定義     Claude 推奨
(B) 新規テーブル master_site_topic
(C) 自動抽出（記事側から動的計算、循環参照リスク）
```

### Claude 推奨: (A) config.js 定数

```javascript
// config.js
const SITE_TOPIC_ENTITIES = {
  cardloan: ['消費者金融', 'カードローン', '実質年率', '限度額', '審査', '即日融資', ...],
  cryptocurrency: ['暗号資産', 'ビットコイン', '取引所', ...],
  securities: ['証券会社', '投資信託', 'NISA', ...],
  fx: ['FX', 'レバレッジ', 'スワップ', ...]
};
```

理由:
```
- 主題エンティティは安定（4 category × 各 10〜20 件 = 40〜80 件規模）
- 変更頻度低い、CRUD UI 不要 → 最小性◎
- partners.json と同様の運用パターン（既存 s-tools と整合）
- 警戒バイアス「機能を盛りたくなる」回避（master_site_topic 新規テーブル却下）
```

### スコア計算

```
記事のエンティティ集合 (KG API 抽出) を E_article
サイト主題エンティティ集合 (config.js) を E_site

topical_alignment_score = |E_article ∩ E_site| / |E_article|

スコア意味:
  1.0  完全整合（記事の全エンティティがサイト主題内）
  0.5  半分整合
  0.0  完全逸脱

閾値: < 0.5 で「トピック逸脱」と判定
```

---

## 5. 4-4: Site Reputation Abuse 防衛 検出 3パターン

```
1. カニバリゼーション (cannibalization)
   - text_similarity (α) ≥ 0.8 の記事ペア
   - 同一 target_query で記事 ≥ 2
   - 影響: 検索結果での自社内競合、SEO 損失

2. トピック逸脱 (topical_drift)
   - topical_alignment_score < 0.5
   - 影響: Site Reputation Abuse スパム判定リスク

3. 大量同質記事 (reputation_abuse_risk)
   - entity_overlap (γ) ≥ 0.7 の記事ペアが 3 以上
   - 影響: ドメイン権威の希薄化、Google 制裁対象
```

### 検出フロー

```
月次バッチ (cron):
  1. master_article_similarity 全レコード再計算（β/γ 含む）
  2. 上記 3 パターンを検出
  3. 該当ケースを master_site_audit_score にレコード作成
  4. リライト履歴タブのサブタブ「サイト全体監査」に表示
  5. Daiki が手動でレビュー → status='resolved'/'ignored'
```

---

## 6. 4-5: 実装位置

### 新規テーブル: master_site_audit_score

```sql
CREATE TABLE master_site_audit_score (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  audit_type TEXT NOT NULL,
  -- 'cannibalization' / 'topical_drift' / 'reputation_abuse_risk'
  target_post_ids TEXT NOT NULL,            -- JSON配列
  score REAL NOT NULL,
  threshold REAL,
  status TEXT NOT NULL DEFAULT 'detected',
  -- 'detected' / 'reviewed' / 'resolved' / 'ignored'
  detection_method TEXT,                    -- 'monthly_batch' / 'manual'
  detected_at DATETIME DEFAULT CURRENT_TIMESTAMP,
  reviewed_by TEXT,
  reviewed_at DATETIME,
  resolved_at DATETIME,
  notes TEXT
);
CREATE INDEX idx_site_audit_type ON master_site_audit_score(audit_type);
CREATE INDEX idx_site_audit_status ON master_site_audit_score(status);
CREATE INDEX idx_site_audit_detected ON master_site_audit_score(detected_at);
```

### API エンドポイント

```
GET  /api/audit/site-audit?audit_type=&status=     監査結果一覧
POST /api/audit/site-audit/recalculate              月次バッチ手動トリガー
PUT  /api/audit/site-audit/:id                      ステータス更新（reviewed/resolved/ignored）
```

### UI 配置: 「リライト履歴」タブ内サブタブ

```
リライト履歴タブ:
  [セッション履歴] [サイト全体監査] [月次バッチ未実行警告]
                       ↑ 論点4 配置
```

理由:
```
- ナビ変更なし、論点1-A 確定の 11タブ最小性維持
- リライト履歴とサイト全体監査は「リライト後の効果検証」で文脈一致
- 警戒バイアス「UI 仕様の過剰精緻化」と整合（独立タブ追加却下）
```

---

## 7. 警戒バイアス対チェック

| バイアス | 対チェック結果 |
|---|---|
| 機能を盛りたくなる | master_site_topic 新規テーブル却下、config.js 定数で代替 |
| テーブル単位 vs システム全体 | master_site_audit_score 1 テーブル追加は責務分離（論点4 監査専用） |
| UI 仕様の過剰精緻化 | 独立タブ「リライト監査」却下、リライト履歴サブタブで吸収 |

---

## 8. 反証プロトコル

### 反証1: 「master_site_topic を新規テーブルで管理した方が CRUD UI 経由で柔軟では？」
主題エンティティは category 別 10〜20 件規模、変更頻度低い。
config.js 定数で十分、新規テーブルは「機能を盛りたくなる」バイアス。

### 反証2: 「Site Reputation Abuse 検出は誤検出リスクが高いのでは？」
status='ignored' で Daiki が誤検出を除外可能。
Daiki 判定が最終ガード（論点2-6 の方針と整合）、自動制裁ではない警告レベル。

### 反証3: 「サイト全体監査を Phase 3 で実装する必然性は？」
案E 旧軸4「構造的健全性」が削除され（IX 章既述）、別レイヤーに分離決定。
分離先がここ。先送りすると Site Reputation Abuse リスクが放置される。
Phase 3 で実装位置確定、Phase 4 で実装。

### 反証4: 「KG API の API 制限 (1000 query/day) で全 3000 記事処理可能か？」
Pair-wise 計算は不要、各記事のエンティティ集合を 1 query で取得。
3000 記事 / 3 日 = 1000 query/day で完了。月次バッチで現実的。

---

## 9. Phase 4 実装時の作業

```
1. shared/entity-extractor-adapter.js 新設
   - Google Knowledge Graph API 連携
   - Adapter パターンで将来差し替え可能
2. node/rewrite/audit/site-auditor.js 新設
   - 月次バッチ、3パターン検出ロジック
3. config.js に SITE_TOPIC_ENTITIES 定数追加
   - 4 category × 各主題エンティティ群を Daiki と確認しつつ整備
4. リライト履歴タブにサブタブ追加 (RewriteHistoryView.jsx 拡張)
   - サイト全体監査画面の実装
5. monitor-jobs.js に月次バッチ cron 追加
   - master_article_similarity 全再計算 + master_site_audit_score 検出
```

---

## 10. 進行記録

```
2026-05-01 セッション継続（論点2 確定後）
  → 論点4 全体構造提示 (5サブ論点)
  → Claude 推奨案を一括提示:
    4-2: (A) Knowledge Graph API
    4-3: (A) config.js 定数
    4-5: (A) リライト履歴サブタブ
  → Daiki 判断: 「判断委任モードで大丈夫そう！」
  → Claude 推奨 (A)(A)(A) で確定
  → knowledge/05 V-G章新設、V章 26テーブル化、V-A章 master_site_audit_score SQL 追加
  → セッション記録（本ファイル）作成
  → main 直接コミット予定
```

---

## 11. 次論点への接続

残論点:
```
論点5  SearchPilot variant 割当ロジック
       (Cloudflare Workers / 1,000記事ずつ control/A/B
        / 統計検定タイミング
        / 論点2-4 の月次バッチ未実行検知の ab_test_id 紐付け実装)
```

論点5 で Phase 3 詳細設計フェーズ完了予定。
論点5 確定後、Phase 4 実装フェーズへの引き継ぎ用 handoff を更新。
