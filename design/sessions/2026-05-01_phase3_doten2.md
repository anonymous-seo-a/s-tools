# Phase 3 詳細設計 論点2: エラーハンドリング設計

日付: 2026-05-01
ステータス: 確定（Daiki 判断委任 + 1点修正指示 → Claude が真=美フレームワーク + 警戒バイアスに基づき確定）
反映先: knowledge/05_rewrite_system_design.md（V-F章新設 / V-A章 master_rewrite_session スキーマ拡張）

---

## 0. 経緯

論点1 / 論点3 確定後、論点2 に進行。
6 サブ論点に分解、Claude 推奨案を一括提示、Daiki が 3点判断。

3 点目（2-6 Compliance 検証エンジン異常時の挙動）で Claude の片側照射バイアスが発動。
Daiki 指摘で訂正、warning モード採用。

---

## 1. 確定事項サマリ

| サブ論点 | 確定 |
|---|---|
| 2-1 LLM API リトライ | 指数バックオフ (max 5回)、Adapter 層に retry ラッパ |
| 2-2 SerpApi フォールバック | キャッシュ参照 → Degraded mode → aborted の3段階 |
| 2-3 WP ロールバック | snapshot 保存 + 失敗時原状復帰、スキーマ3列追加 |
| 2-4 月次バッチ未実行検知 | applied_to_wp=1 AND ab_test_id IS NULL の14日経過監視（実装詳細は論点5 で） |
| 2-5 queue_session_link 保証 | トランザクションラッパで必須リンク作成 |
| 2-6 Compliance 検証エンジン異常 | **warning モード**（処理継続、Daiki 判定で最終ガード） |

status 列拡張: `'compliance_checking'` / `'wp_applying'` / `'wp_apply_rolled_back'` 追加。

---

## 2. 2-1: LLM API 失敗時のリトライ戦略

```
失敗パターン × 対応:
  429 Rate Limit       → 指数バックオフ (1s→2s→4s→8s→16s, max 5回)
  5xx Server Error     → 指数バックオフ (max 5回)
  ネットワークエラー    → 指数バックオフ (max 5回)
  タイムアウト         → 短バックオフ + 1回リトライ
  JSON パースエラー    → 1回リトライ（プロンプト「正しい JSON 形式で」強調）

全リトライ失敗時:
  master_rewrite_session.status = 'aborted'
  master_rewrite_session.notes に失敗詳細 (JSON: error_type, attempts, last_error_at)
  Daiki 通知: リライト履歴タブで失敗状態を表示
```

実装位置: `shared/llm-adapters/*-adapter.js` に retry ラッパ組込（Anthropic / OpenAI / Gemini 共通パターン）。

---

## 3. 2-2: SerpApi 失敗時のフォールバック

```
失敗パターン × 対応:
  月次クォータ超過    → 即時フォールバック（リトライしない）
  一時的 API 障害      → 指数バックオフ (max 3回)
  特定クエリ異常       → 1回リトライ

フォールバック戦略 (3段階):
  Step 1: master_competitor_corpus.crawled_at が直近30日以内 → 既存データ使用
  Step 2: キャッシュなし → Degraded mode
          Query Fan-out 工程をスキップ、既存データのみで案C 実行
          notes に 'serpapi_unavailable_degraded' 記録
  Step 3: 全失敗 → status='aborted'、Daiki 通知
```

実装位置: `shared/serpapi-adapter.js`（新規、案D 3.H Archive Adapter と同パターン）。

---

## 4. 2-3: WordPress 反映失敗時のロールバック

### スキーマ拡張（master_rewrite_session に 3 列追加）

```sql
ALTER TABLE master_rewrite_session ADD COLUMN wp_snapshot_before_apply TEXT;
ALTER TABLE master_rewrite_session ADD COLUMN wp_apply_started_at DATETIME;
ALTER TABLE master_rewrite_session ADD COLUMN wp_apply_completed_at DATETIME;
```

### ロールバック機構

```
反映前: WP 記事の現在 HTML をスナップショット保存
        master_rewrite_session.wp_snapshot_before_apply

反映実行中: status='wp_applying'、各 diff の applied_to_wp=1
            wp_apply_started_at 記録

致命的失敗時: スナップショットから WP に POST して原状復帰
              全 diff の applied_to_wp=0 に戻す
              status='wp_apply_rolled_back'

スナップショット管理: 3ヶ月後に NULL クリア（cron）、ストレージ膨張対策
```

実装位置: `node/rewrite/wp-applier.js`（新規）+ 既存 `gap-fill.js updateWpPost()` 流用。

### 反証（既出済）

```
反証: 「snapshot を保存するとストレージが膨らむのでは？」
  → 月50本ペース、snapshot 数万バイト/件 → 月数MB。SQLite で問題なし。
    3ヶ月後に NULL クリア。

反証: 「部分失敗時もロールバックすべきでは？」
  → WP REST API は記事単位 Atomic Update。1記事内の複数 diff は一括 PATCH、
    部分失敗しない。複数記事跨ぎは別 session、別 snapshot。
```

---

## 5. 2-4: 月次バッチ未実行検知

```
検知対象:
  applied_to_wp=1 (リライト反映済)
  AND ab_test_id IS NULL (A/Bテスト未エントリー)
  AND applied_at < NOW() - 14 日

検知ロジック (日次 cron、既存 monitor-jobs.js に追加):
  該当があれば管理画面の「リライト履歴」タブにバナー警告
  Daiki が手動で A/Bテスト割当 or 14日延長

注: master_rewrite_diff の ab_test_id 紐付け実装は論点5 (SearchPilot variant 割当) で確定。
    論点2 では検知ロジックの構造のみ確定。
```

---

## 6. 2-5: queue_session_link 「最低1件のリンク」アプリケーションレベル保証

```
session 作成時にトランザクション内で必ずリンクも作成:

BEGIN TRANSACTION;
  INSERT INTO master_rewrite_session (...) RETURNING id;
  INSERT INTO master_rewrite_queue_session_link (queue_id, session_id) VALUES (?, ?);
COMMIT;

リンク作成失敗 → トランザクション全体 ROLLBACK → session 作成も巻き戻し

既存 session への queue 追加 (1対多、案D 4.M' 確定):
  INSERT INTO master_rewrite_queue_session_link (queue_id, session_id)
  ON CONFLICT(queue_id, session_id) DO NOTHING;
```

実装位置: `node/rewrite/session-creator.js`（新規）に集約。
全 session 作成は必ずこのモジュール経由。

---

## 7. 2-6: Compliance 検証エンジン異常時のフロー（warning モード）

### Claude 初期推奨と Daiki 訂正

**Claude 初期推奨**: session 中止（YMYL 安全側ルール一辺倒）
**Daiki 指摘**: 「warning にしよう。完動することがだいじ。」
**訂正後**: warning モード（処理継続、Daiki 判定で最終ガード）

### 確定仕様

```
異常パターン:
  master_rules SELECT エラー（DB接続失敗）
  正規表現実行例外（malformed pattern）
  checker 内部例外

対応戦略 (warning モード):
  master_rewrite_session.status は変更しない（処理継続、'awaiting_diff_judgment' へ）
  master_rewrite_session.notes に異常詳細記録 (JSON: compliance_check_error)
  差分判定画面ヘッダに警告サブパネル表示:
    [規約違反 ⚠ 検証エラー]
  Daiki が判定時に把握、手動で master_rules を確認 or 警告認識のうえ判定
```

### 設計判断（修正版）

```
- システム完動性 > 検証完璧性
  Compliance 検証はシステム側の都合 (DB接続/正規表現例外 等) で異常になる
  リライト本体は機能するため、検証エンジン異常で全体停止は最小性違反

- Daiki 判定の最終ガード
  YMYL 安全性は Daiki 判定（人間最終承認、案N 不採用原則）で担保
  Compliance 検証は補助、Daiki 判定が主

- 異常記録は確実に
  notes に記録 + ヘッダ警告で Daiki が見落とさない仕組み
```

---

## 8. 警戒バイアス対チェック（Claude 自己批判）

### 「自分の初期推奨に固着するバイアス」発動

```
Claude 初期推奨: session 中止 (YMYL 安全側ルール一辺倒)

片側照射の内容:
  - 「YMYL 安全側」というルールを機械的に適用
  - Daiki 判定が最終ガードとして機能する事実を見落とした
  - 「Compliance 検証スキップ = YMYL 致命的」という単純化
  - システム側の都合 (検証エンジン異常) と運用側の都合 (Daiki 判定) を混同

Daiki 指摘で気付き:
  - 「完動することがだいじ」= システム全体の最小性
  - Daiki 判定がある以上、検証スキップでも YMYL 安全性は担保される
  - 検証エンジン異常で全体停止は過剰反応 = 最小性違反
```

### 過去の同種バイアス事例

```
論点1-B 編集UI:
  Claude 初期推奨 (a) plain textarea を独断で選んだ
  Daiki 指摘で 5案再提示、(e) inline diff editor に訂正

共通点:
  - Claude が単一ルールで機械的判断
  - Daiki の直感が片側照射を暴く
  - 訂正後の方が真=美 3軸全てで上回る
```

### 学習

```
警戒バイアス「自分の初期推奨に固着」(案D 既出) は Phase 3 でも頻発。
- Daiki の直感的判断は重要シグナル（CLAUDE.md II 章既述）
- Claude が「ルール適用」で完結させようとする時、片側照射の可能性
- 反証プロトコル（CLAUDE.md IX 章）の徹底適用が必要
```

---

## 9. status 列の最終形

```
master_rewrite_session.status:
  'planned'                         初期化
  'analyzing'                       Opus 4.7 分析中
  'awaiting_policy_judgment'        案K発動、Daiki 方針判定待ち
  'generating'                      Sonnet 4.6 差分生成中
  'compliance_checking'             Compliance 事後検証中 (新規、2-6)
  'awaiting_diff_judgment'          差分単位 Daiki 判定待ち
  'wp_applying'                     WP 反映中 (新規、2-3)
  'completed'                       全 diff 反映完了
  'aborted'                         任意の段階で中止 (LLM/SerpApi致命的失敗)
  'wp_apply_rolled_back'            WP 反映失敗、ロールバック実行 (新規、2-3)

★ 'compliance_check_failed' は不採用（warning モードのため status 変更しない）
```

---

## 10. Phase 4 実装時の作業

```
1. shared/llm-adapters/*-adapter.js に retry ラッパ組込
2. shared/serpapi-adapter.js 新設（フォールバック機構付）
3. node/rewrite/wp-applier.js 新設（snapshot + ロールバック）
4. node/rewrite/session-creator.js 新設（トランザクションラッパ）
5. node/rewrite/llm-execution/compliance-checker.js に warning モード組込
6. monitor-jobs.js に月次バッチ未実行検知 cron 追加
7. リライト履歴タブのバナー警告 UI
8. wp_snapshot_before_apply の 3ヶ月クリア cron
9. master_rewrite_session スキーマ拡張 (wp_snapshot_before_apply / wp_apply_started_at /
   wp_apply_completed_at の 3 列追加)
```

---

## 11. 進行記録

```
2026-05-01 セッション継続（論点3 確定後）
  → 論点2 全体構造提示 (6サブ論点)
  → Claude 推奨案を一括提示
  → Daiki 判断:
    1. OK (2-1〜2-5)
    2. OK (スキーマ拡張)
    3. warning にしよう。完動することがだいじ。
  → Claude 片側照射バイアス気付き、自己批判
  → 2-6 を warning モードに修正
  → status 列の最終形整理 (compliance_check_failed 不採用)
  → knowledge/05 V-F章新設、V-A章 master_rewrite_session スキーマ拡張、
    status コメント更新
  → セッション記録（本ファイル）作成
  → main 直接コミット予定
```

---

## 12. 次論点への接続

残論点:
```
論点4  サイト全体監査レイヤー設計
       (旧軸4 構造的健全性の移行先 / Site Reputation Abuse 防衛 / topical 整合性)
論点5  SearchPilot variant 割当ロジック
       (Cloudflare Workers / 1,000記事ずつ control/A/B / 統計検定タイミング
        / 月次バッチ未実行検知の ab_test_id 紐付け実装)
```

論点5 は論点2-4 の月次バッチ未実行検知の実装詳細を含むため、論点4 → 論点5 の順が自然。
ただし論点4 と論点5 は独立タスクなので、Daiki の判断次第。

次セッション開始時、Daiki に「論点4 / 論点5」のどちらから進めるか選択を仰ぐ。
